# Grammar

Allowed categories: `Grammar`, `Spelling`, `Punctuation`. Do not report semantic accuracy or stylistic preference. Apply target-language spacing and typography rules only when the bound language notes support them.
